# Secrets — never commit
.env
app-secrets.json

# SSH private keys — never commit (only box.sh + known_hosts are tracked under .ssh/)
.ssh/id_*
.ssh/*rumi_netcup*
*.pem
*.key
*.pfx
*.p12

# Local backups created by on-box edits
*.bak
*.bak.*
